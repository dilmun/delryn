//! Shared UI primitives reused across views and overlays — widgets with no
//! business logic, so the same interaction behaves identically everywhere.

pub mod text_input;

pub use text_input::TextInput;
